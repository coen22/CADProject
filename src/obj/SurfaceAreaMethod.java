package obj;

public interface SurfaceAreaMethod {
	/**
	 * Method to calculate the surface area of an object
	 * @param a 3D object
	 * @return the surface area
	 */
	public double getSurfaceArea(Object3D object3d);
}
